# When

- The `when keyword` in Kotlin is a powerful `control-flow` construct used for conditional branching, similar to a `switch` statement

### Syntax

```kotlin
when (expression){
   conditionOne -> print();
   conditionTwo -> print();
   else -> print(); // default
}
```
